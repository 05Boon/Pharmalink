# Pharmacy Inventory Pooling

This workspace is set up with Flutter as the frontend.

## What is in this repository

- Frontend (active): Flutter app in `flutter_app`.
- Workspace root: orchestration scripts and docs only.

## Frontend

The active frontend source is in `flutter_app/lib`.

### Run frontend

```bash
cd flutter_app
flutter pub get
flutter run -d chrome
```
