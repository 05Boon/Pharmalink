{
  "name": "pharmacy-inventory-pooling",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd flutter_app && flutter run -d chrome",
    "dev:flutter": "cd flutter_app && flutter run -d chrome",
    "test:flutter": "cd flutter_app && flutter test"
  }
}