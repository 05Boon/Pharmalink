services:
  database:
    image: postgis/postgis:15-3.3
    container_name: local_pharmacy_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: local_secret_password
      POSTGRES_DB: pharmacy_network_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d pharmacy_network_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - pharmalink-network

  web:
    build:
      context: ./routing_engine
      dockerfile: Dockerfile
    container_name: routing_engine_web
    ports:
      - "8000:8000"
    depends_on:
      database:
        condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    env_file:
      - ./routing_engine/.env
    environment:
      - TEST_DATABASE_URL=postgresql+asyncpg://admin:local_secret_password@database:5432/pharmacy_network_db
    networks:
      - pharmalink-network

volumes:
  pgdata:

networks:
  pharmalink-network:
    driver: bridge